# Port learnings — <APP>

Date:
Ported by: Claude Code on the web, session <n>
Tier: static | SQLite-backed | outlier

## What I assumed that was wrong
One line per correction. What I believed, what the code actually did, how I found out.

## Where the iterations went
Which steps took more than one attempt, and why. Rank them by time cost.

## Gotchas discovered
Only things a future run would hit too. Each one needs the command that reveals it.

## What should become a template
Any file here that is now generic enough to promote into `templates/`, and what
would have to change per app.

## Proposed playbook edits
Concrete diff-style edits to PORTING-PLAYBOOK.md. Promote ASSUMED lines that this
run verified. Delete lines this run refuted. Add new ones.

## Open questions for Jelle
Things I could not decide or could not see from the sandbox.

## What only showed up on the real server
(Jelle fills this in after deploying. Leave empty.)
